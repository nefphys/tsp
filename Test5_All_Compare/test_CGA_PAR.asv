tarTsp = dir("data");
tarTsp = tarTsp(3:end);

%CGA 参数
tpath = tarTsp(2).folder;
tspData = [tpath '\' tarTsp(2).name];
[Distance,City] = readfile(tspData,1);


Ctime = [];
for i = 1:20
    varargin = struct('xy',City, 'popSize', 100, 'numIter', 1e3);
    CGA_TSP_Solve_Struct = CGA_Solver(varargin);
    Ctime(i) = 
end


varargin = struct('xy',City, 'popSize', 100, 'numIter', 1e3);
CGA_TSP_Solve_Struct = CGA_Solver(varargin);